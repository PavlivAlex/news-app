import { variables } from "../../assets/styles/variables";

export const styles = {
  height: "calc(100% - 60px)",
  welcomeText: {
    padding: `0 ${variables.paddingXL} ${variables.paddingMega} ${variables.paddingXL}`,
  },
};
